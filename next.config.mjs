/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [], // Add any remote image domains here if needed
    unoptimized: false, // Enable image optimization
  },
};

export default nextConfig;
